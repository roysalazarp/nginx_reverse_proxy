# RUN
version: "3.3"
services:
  nginx-reverse-proxy:
    image: roysalazarportilla/nginx-reverse-proxy:latest
    restart: always
    ports:
      - '80:80'
    container_name: nginx-reverse-proxy
    networks:
      - my-network
networks:
  my-network:
    external: true